/* --- TRANSLATION DICTIONARY --- */
      const translations = {
        en: {
          resp: "PLAY RESPONSIBLY", donate: "UPGRADE ELITE", net_profit: "Net Profit", start_bal: "Starting Balance", win_rate: "Win Rate", recent_form: "Recent Form", target_goal: "Target Goal (RON)", realized: "realized", cashout_reset: "CASHOUT (RESET)", filter_all: "ALL", filter_pending: "PENDING", filter_win: "WIN", filter_loss: "LOSS", sport_all: "ALL SPORTS", sport_foot: "FOOTBALL", sport_ten: "TENNIS", sport_basket: "BASKETBALL", sport_other: "OTHER", sport_foot_short: "Football", sport_ten_short: "Tennis", sport_basket_short: "Basketball", sport_other_short: "Other", place_new: "PLACE NEW TICKET", match_name: "Match / Selection", odds: "Odds", events_on: "EVENTS ON TICKET", total_odds: "Total Odds:", conf_meter: "CONFIDENCE METER", ticket_name: "Ticket name (optional)", stake: "Stake (RON)", add_photo: "ADD TICKET PHOTO", no_file: "No file selected", photo_added: "Photo added!", risk_meter: "RISK METER", risk_low: "LOW RISK", risk_med: "MEDIUM RISK", risk_high: "HIGH RISK! CAUTION", add_ticket: "ADD TICKET", perf_last5: "PERFORMANCE (LAST 5)", active_hist: "ACTIVE & HISTORY TICKETS", no_tickets: "NO TICKETS FOUND", miza: "Stake:", payout: "Payout:", cashout_txt: "Cashout:", btn_win: "WIN", btn_cashout: "CASHOUT", btn_loss: "LOSS", err_odds: "Valid name and odds > 1 required!", err_stake: "Enter valid stake!", err_event: "Add at least one event!", prompt_cashout: "Enter cashout amount (RON):", alert_bal: "rGdbet: Add balance!", sport_football: "FOOTBALL", sport_tennis: "TENNIS", sport_basketball: "BASKETBALL", sport_other: "OTHER", income_calc: "INCOME CALCULATOR", calc_btn: "CALCULATE", calc_result_label: "Period Profit:", calendar_title: "PROFIT CALENDAR", create_port: "+ Create Portfolio...", prompt_name: "New portfolio name:", prompt_bank: "Initial bank (RON):", main_bank: "Main Bank", total_budget: "Total Budget", total_tickets: "Total Tickets", sport_stats_title: "SPORT DISTRIBUTION", win_streak: "Win Streak", bh_title: "BANKROLL HEALTH & ANALYTICS", bh_roi: "ROI (RETURN ON INV.)", bh_yield: "YIELD", bh_staked: "TOTAL STAKED", bh_drawdown: "MAX DRAWDOWN", ticket: "Ticket"
        },
        ro: {
          resp: "PARIAZĂ RESPONSABIL", donate: "FĂ-ȚI UPGRADE!", net_profit: "Profit Net", start_bal: "Balanță Start", win_rate: "Rată Câștig", recent_form: "Formă Recentă", target_goal: "Obiectiv (RON)", realized: "realizat", cashout_reset: "CASHOUT (RESETARE)", filter_all: "TOATE", filter_pending: "ÎN AȘTEPTARE", filter_win: "CÂȘTIG", filter_loss: "PIERDUT", sport_all: "TOATE SPORTURILE", sport_foot: "FOTBAL", sport_ten: "TENIS", sport_basket: "BASCHET", sport_other: "ALTELE", sport_foot_short: "Fotbal", sport_ten_short: "Tenis", sport_basket_short: "Baschet", sport_other_short: "Altele", place_new: "PLASEAZĂ BILET NOU", match_name: "Nume Meci / Selecție", odds: "Cotă", events_on: "Categorie", total_odds: "Cotă Totală:", conf_meter: "NIVEL DE ÎNCREDERE", ticket_name: "Nume bilet (opțional)", stake: "Miză (RON)", add_photo: "ADAUGĂ POZĂ", no_file: "Niciun fișier selectat", photo_added: "Poză adăugată!", risk_meter: "NIVEL DE RISC", risk_low: "RISC SCĂZUT", risk_med: "RISC MEDIU", risk_high: "RISC RIDICAT! ATENȚIE", add_ticket: "ADAUGĂ BILET", perf_last5: "PERFORMANȚĂ (ULTIMELE 5)", active_hist: "BILETE ACTIVE & ISTORIC", no_tickets: "NICIUN BILET GĂSIT", miza: "Miză:", payout: "Câștig:", cashout_txt: "Cashout:", btn_win: "CÂȘTIG", btn_cashout: "CASHOUT", btn_loss: "PIERDUT", err_odds: "Introduceți nume și cotă > 1!", err_stake: "Introduceți o miză validă!", err_event: "Adăugați minim un EVENIMENT!", prompt_cashout: "Suma de cashout (RON):", alert_bal: "rGdbet: Adaugă balanță!", sport_football: "FOTBAL", sport_tennis: "TENIS", sport_basketball: "BASCHET", sport_other: "ALTELE", income_calc: "CALCULATOR VENIT", calc_btn: "CALCULEAZĂ", calc_result_label: "Profit Perioadă:", calendar_title: "CALENDAR PROFIT", create_port: "+ Creare Portofoliu...", prompt_name: "Nume portofoliu nou:", prompt_bank: "Bancă inițială (RON):", main_bank: "Banca Principală", total_budget: "Buget Total", total_tickets: "Total Bilete", sport_stats_title: "DISTRIBUȚIE SPORTURI", win_streak: "Serie Victorie", bh_title: "BANKROLL HEALTH & ANALYTICS", bh_roi: "ROI (RETURN ON INV.)", bh_yield: "YIELD (RANDAMENT)", bh_staked: "TOTAL MIZAT", bh_drawdown: "MAX DRAWDOWN", ticket: "Bilet"
        },
        it: {
          resp: "GIOCA RESPONSABILMENTE", donate: "DONA REVOLUT", net_profit: "Profitto Netto", start_bal: "Saldo Iniziale", win_rate: "Vittorie", recent_form: "Forma", target_goal: "Obiettivo (RON)", realized: "realizzato", cashout_reset: "INCASSA (RESET)", filter_all: "TUTTI", filter_pending: "IN ATTESA", filter_win: "VINTI", filter_loss: "PERSI", sport_all: "TUTTI GLI SPORT", sport_foot: "CALCIO", sport_ten: "TENNIS", sport_basket: "BASKET", sport_other: "ALTRO", sport_foot_short: "Calcio", sport_ten_short: "Tennis", sport_basket_short: "Basket", sport_other_short: "Altro", place_new: "NUOVO BIGLIETTO", match_name: "Nome Partita", odds: "Quota", events_on: "EVENTI SUL BIGLIETTO", total_odds: "Quota Totale:", conf_meter: "LIVELLO FIDUCIA", ticket_name: "Nome biglietto", stake: "Puntata (RON)", add_photo: "AGGIUNGI FOTO", no_file: "Nessun file", photo_added: "Foto aggiunta!", risk_meter: "LIVELLO RISCHIO", risk_low: "RISCHIO BASSO", risk_med: "RISCHIO MEDIO", risk_high: "RISCHIO ALTO!", add_ticket: "AGGIUNGI BIGLIETTO", perf_last5: "RENDIMENTO (ULTIME 5)", active_hist: "BIGLIETTI ATTIVI E STORICO", no_tickets: "NESSUN BIGLIETTO", miza: "Puntata:", payout: "Vincita:", cashout_txt: "Incasso:", btn_win: "VINTA", btn_cashout: "INCASSA", btn_loss: "PERSA", err_odds: "Inserisci nome e quota > 1!", err_stake: "Puntata valida!", err_event: "Aggiungi un evento!", prompt_cashout: "Importo incasso (RON):", alert_bal: "rGdbet: Aggiungi saldo!", sport_football: "CALCIO", sport_tennis: "TENNIS", sport_basketball: "BASKET", sport_other: "ALTRO", income_calc: "CALCOLATORE ENTRATE", calc_btn: "CALCOLA", calc_result_label: "Profitto periodo:", calendar_title: "CALENDARIO PROFITTO", create_port: "+ Crea Portfolio...", prompt_name: "Nome nuovo portfolio:", prompt_bank: "Banca iniziale (RON):", main_bank: "Banca Principale", total_budget: "Budget Totale", total_tickets: "Totale Biglietti"
        },
        es: {
          resp: "JUEGA CON RESPONSABILIDAD", donate: "DONAR REVOLUT", net_profit: "Beneficio Neto", start_bal: "Saldo Inicial", win_rate: "Tasa Victorias", recent_form: "Forma", target_goal: "Objetivo (RON)", realized: "logrado", cashout_reset: "RETIRAR (RESET)", filter_all: "TODOS", filter_pending: "PENDIENTE", filter_win: "GANADO", filter_loss: "PERDIDO", sport_all: "TODOS LOS DEPORTES", sport_foot: "FÚTBOL", sport_ten: "TENIS", sport_basket: "BALONCESTO", sport_other: "OTROS", sport_foot_short: "Fútbol", sport_ten_short: "Tenis", sport_basket_short: "Baloncesto", sport_other_short: "Otros", place_new: "NUEVO BOLETO", match_name: "Nombre Partido", odds: "Cuota", events_on: "EVENTOS EN BOLETO", total_odds: "Cuota Total:", conf_meter: "NIVEL CONFIANZA", ticket_name: "Nombre boleto", stake: "Apuesta (RON)", add_photo: "AÑADIR FOTO", no_file: "Sin archivo", photo_added: "¡Foto añadida!", risk_meter: "NIVEL DE RIESGO", risk_low: "RIESGO BAJO", risk_med: "RIESGO MEDIO", risk_high: "¡RIESGO ALTO!", add_ticket: "AÑADIR BOLETO", perf_last5: "RENDIMIENTO (ÚLTIMAS 5)", active_hist: "BOLETOS ACTIVOS E HISTORIAL", no_tickets: "SIN BOLETOS", miza: "Apuesta:", payout: "Ganancia:", cashout_txt: "Retiro:", btn_win: "GANADO", btn_cashout: "RETIRAR", btn_loss: "PERDIDO", err_odds: "¡Nombre y cuota > 1!", err_stake: "¡Apuesta válida!", err_event: "¡Añade un evento!", prompt_cashout: "Importo a retirar (RON):", alert_bal: "rGdbet: ¡Añade saldo!", sport_football: "FÚTBOL", sport_tennis: "TENIS", sport_basketball: "BALONCESTO", sport_other: "OTROS", income_calc: "CALCULADORA INGRESOS", calc_btn: "CALCULAR", calc_result_label: "Beneficio período:", calendar_title: "CALENDARIO BENEFICIOS", create_port: "+ Crear Portafolio...", prompt_name: "Nombre portafolio:", prompt_bank: "Banco inicial (RON):", main_bank: "Banco Principal", total_budget: "Presupuesto Total", total_tickets: "Total Boletos"
        },
        de: {
          resp: "VERANTWORTUNGSBEWUSST SPIELEN", donate: "REVOLUT SPENDEN", net_profit: "Nettogewinn", start_bal: "Startguthaben", win_rate: "Gewinnquote", recent_form: "Letzte Form", target_goal: "Ziel (RON)", realized: "erreicht", cashout_reset: "AUSZAHLEN (RESET)", filter_all: "ALLE", filter_pending: "AUSSTEHEND", filter_win: "GEWONNEN", filter_loss: "VERLOREN", sport_all: "ALLE SPORTARTEN", sport_foot: "FUSSBALL", sport_ten: "TENNIS", sport_basket: "BASKETBALL", sport_other: "ANDERE", sport_foot_short: "Fußball", sport_ten_short: "Tennis", sport_basket_short: "Basketball", sport_other_short: "Andere", place_new: "NEUES TICKET", match_name: "Spielname", odds: "Quote", events_on: "EVENTS AUF TICKET", total_odds: "Gesamtquote:", conf_meter: "VERTRAUENSLEVEL", ticket_name: "Ticketname", stake: "Einsatz (RON)", add_photo: "FOTO HINZUFÜGEN", no_file: "Kein Datei", photo_added: "Foto hinzugefügt!", risk_meter: "RISIKO-LEVEL", risk_low: "GERINGES RISIKO", risk_med: "MITTLERES RISIKO", risk_high: "HOHES RISIKO!", add_ticket: "TICKET HINZUFÜGEN", perf_last5: "LEISTUNG (LETZTE 5)", active_hist: "AKTIVE & VERGANGENE TICKETS", no_tickets: "KEINE TICKETS", miza: "Einsatz:", payout: "Gewinn:", cashout_txt: "Auszahlung:", btn_win: "GEWONNEN", btn_cashout: "AUSZAHLEN", btn_loss: "VERLOREN", err_odds: "Gültiger Name & Quote > 1!", err_stake: "Gültiger Einsatz!", err_event: "Mindestens ein Event!", prompt_cashout: "Auszahlungsbetrag (RON):", alert_bal: "rGdbet: Guthaben hinzufügen!", sport_football: "FUSSBALL", sport_tennis: "TENNIS", sport_basketball: "BASKETBALL", sport_other: "ANDERE", income_calc: "EINKOMMENSRECHNER", calc_btn: "BERECHNEN", calc_result_label: "Periodengewinn:", calendar_title: "PROFITKALENDER", create_port: "+ Portfolio erstellen...", prompt_name: "Name des Portfolios:", prompt_bank: "Anfangsbank (RON):", main_bank: "Hauptbank", total_budget: "Gesamtbudget", total_tickets: "Gesamttickets"
        },
        pt: {
          resp: "JOGUE COM RESPONSABILIDADE", donate: "DOAR REVOLUT", net_profit: "Lucro Líquido", start_bal: "Saldo Inicial", win_rate: "Taxa de Vitória", recent_form: "Forma", target_goal: "Objetivo (RON)", realized: "realizado", cashout_reset: "LEVANTAR (RESET)", filter_all: "TODOS", filter_pending: "PENDENTE", filter_win: "GANHO", filter_loss: "PERDIDO", sport_all: "TODOS OS DESPORTOS", sport_foot: "FUTEBOL", sport_ten: "TÉNIS", sport_basket: "BASQUETEBOL", sport_other: "OUTROS", sport_foot_short: "Futebol", sport_ten_short: "Ténis", sport_basket_short: "Basquetebol", sport_other_short: "Outros", place_new: "NOVO BILHETE", match_name: "Nome do Jogo", odds: "Cota", events_on: "EVENTOS NO BILHETE", total_odds: "Cota Total:", conf_meter: "CONFIANÇA", ticket_name: "Nome bilhete", stake: "Aposta (RON)", add_photo: "ADICIONAR FOTO", no_file: "Nenhum arquivo", photo_added: "Foto adicionada!", risk_meter: "NÍVEL DE RISCO", risk_low: "RISCO BAIXO", risk_med: "RISCO MÉDIO", risk_high: "ALTO RISCO!", add_ticket: "ADICIONAR BILHETE", perf_last5: "DESEMPENHO (ÚLTIMAS 5)", active_hist: "BILHETES ATIVOS E HISTÓRICO", no_tickets: "NENHUM BILHETE", miza: "Aposta:", payout: "Ganho:", cashout_txt: "Levantamento:", btn_win: "GANHO", btn_cashout: "LEVANTAR", btn_loss: "PERDIDO", err_odds: "Insira nome e cota > 1!", err_stake: "Aposta válida!", err_event: "Adicione um evento!", prompt_cashout: "Valor levantamento (RON):", alert_bal: "rGdbet: Adicione saldo!", sport_football: "FUTEBOL", sport_tennis: "TÉNIS", sport_basketball: "BASQUETEBOL", sport_other: "OUTROS", income_calc: "CALCULADORA RENDAS", calc_btn: "CALCULAR", calc_result_label: "Lucro do período:", calendar_title: "CALENDÁRIO LUCRO", create_port: "+ Criar Portfólio...", prompt_name: "Novo nome:", prompt_bank: "Banco inicial (RON):", main_bank: "Banco Principal", total_budget: "Orçamento Total", total_tickets: "Total Bilhetes"
        },
        ru: {
          resp: "ИГРАЙТЕ ОТВЕТСТВЕННО", donate: "ДОНАТ REVOLUT", net_profit: "Чистая прибыль", start_bal: "Начальный баланс", win_rate: "Победы", recent_form: "Форма", target_goal: "Цель (RON)", realized: "выполнено", cashout_reset: "ВЫВОД (СБРОС)", filter_all: "ВСЕ", filter_pending: "ОЖИДАЕТ", filter_win: "ВЫИГРЫШ", filter_loss: "ПРОИГРЫШ", sport_all: "ВСЕ ВИДЫ", sport_foot: "ФУТБОЛ", sport_ten: "ТЕННИС", sport_basket: "БАСКЕТБОЛ", sport_other: "ДРУГОЕ", sport_foot_short: "Футбол", sport_ten_short: "Теннис", sport_basket_short: "Баскетбол", sport_other_short: "Другое", place_new: "НОВЫЙ БИЛЕТ", match_name: "Название матча", odds: "Коэфф.", events_on: "СОБЫТИЯ В БИЛЕТЕ", total_odds: "Общий коэфф.:", conf_meter: "УВЕРЕННОСТЬ", ticket_name: "Имя билета", stake: "Ставка (RON)", add_photo: "ДОБАВИТЬ ФОТО", no_file: "Файл не выбран", photo_added: "Фото добавлено!", risk_meter: "УРОВЕНЬ РИСКА", risk_low: "НИЗКИЙ РИСК", risk_med: "СРЕДНИЙ РИСК", risk_high: "ВЫСОКИЙ РИСК!", add_ticket: "ДОБАВИТЬ БИЛЕТ", perf_last5: "РЕЗУЛЬТАТ (ПОСЛЕДНИЕ 5)", active_hist: "АКТИВНЫЕ И ИСТОРИЯ", no_tickets: "БИЛЕТЫ НЕ НАЙДЕНЫ", miza: "Ставка:", payout: "Выплата:", cashout_txt: "Вывод:", btn_win: "ВЫИГРЫШ", btn_cashout: "ВЫВОД", btn_loss: "ПРОИГРЫШ", err_odds: "Имя и коэфф. > 1!", err_stake: "Корректная ставка!", err_event: "Хотя бы одно событие!", prompt_cashout: "Сумма вывода (RON):", alert_bal: "rGdbet: Пополните баланс!", sport_football: "ФУТБОЛ", sport_tennis: "ТЕННИС", sport_basketball: "БАСКЕТБОЛ", sport_other: "ДРУГОЕ", income_calc: "КАЛЬКУЛЯТОР ДОХОДА", calc_btn: "РАССЧИТАТЬ", calc_result_label: "Прибыль за период:", calendar_title: "КАЛЕНДАРЬ ПРИБЫЛИ", create_port: "+ Создать Портфель...", prompt_name: "Имя портфеля:", prompt_bank: "Начальный банк (RON):", main_bank: "Главный Банк", total_budget: "Общий бюджет", total_tickets: "Всего билетов"
        },
        bg: {
          resp: "ИГРАЙТЕ ОТГОВОРНО", donate: "ДАРЕНИЕ REVOLUT", net_profit: "Нетна печалба", start_bal: "Начален баланс", win_rate: "Победи", recent_form: "Форма", target_goal: "Цел (RON)", realized: "постигнато", cashout_reset: "ИЗТЕГЛЯНЕ (НУЛИРАНЕ)", filter_all: "ВСИЧКИ", filter_pending: "ЧАКАЩИ", filter_win: "ПЕЧЕЛИВШИ", filter_loss: "ЗАГУБЕНИ", sport_all: "ВСИЧКИ", sport_foot: "ФУТБОЛ", sport_ten: "ТЕНИС", sport_basket: "БАСКЕТБОЛ", sport_other: "ДРУГИ", sport_foot_short: "Футбол", sport_ten_short: "Тенис", sport_basket_short: "Баскетбол", sport_other_short: "Други", place_new: "НОВ ФИШ", match_name: "Име на мача", odds: "Коеф.", events_on: "СЪБИТИЯ ВЪВ ФИША", total_odds: "Общ коеф.:", conf_meter: "УВЕРЕНОСТ", ticket_name: "Име на фиша", stake: "Залог (RON)", add_photo: "ДОБАВИ СНИМКА", no_file: "Няма файл", photo_added: "Снимката е добавена!", risk_meter: "НИВО НА РИСК", risk_low: "НИСЪК РИСК", risk_med: "СРЕДЕН РИСК", risk_high: "ВИСОК РИСК!", add_ticket: "ДОБАВИ ФИШ", perf_last5: "ПРЕДСТАВЯНЕ (ПОСЛЕДНИ 5)", active_hist: "АКТИВНИ И ИСТОРИЯ", no_tickets: "НЯМА ФИШОВЕ", miza: "Залог:", payout: "Печалба:", cashout_txt: "Изтегляне:", btn_win: "ПЕЧЕЛИВШ", btn_cashout: "ИЗТЕГЛЯНЕ", btn_loss: "ЗАГУБЕН", err_odds: "Име и коеф. > 1!", err_stake: "Валиден залог!", err_event: "Поне едно събитие!", prompt_cashout: "Сума (RON):", alert_bal: "rGdbet: Добави баланс!", sport_football: "ФУТБОЛ", sport_tennis: "ТЕНИС", sport_basketball: "БАСКЕТБОЛ", sport_other: "ДРУГИ", income_calc: "КАЛКУЛАТОР ДОХОДИ", calc_btn: "ИЗЧИСЛИ", calc_result_label: "Печалба период:", calendar_title: "КАЛЕНДАР ПЕЧАЛБА", create_port: "+ Създай Портфолио...", prompt_name: "Име:", prompt_bank: "Начална банка (RON):", main_bank: "Главна Банка", total_budget: "Общ бюджет", total_tickets: "Общо фишове"
        },
        zh: {
          resp: "理性游戏", donate: "捐赠 REVOLUT", net_profit: "净利润", start_bal: "初始余额", win_rate: "胜率", recent_form: "近期表现", target_goal: "目标 (RON)", realized: "已完成", cashout_reset: "提现 (重置)", filter_all: "全部", filter_pending: "待定", filter_win: "赢", filter_loss: "输", sport_all: "所有运动", sport_foot: "足球", sport_ten: "网球", sport_basket: "篮球", sport_other: "其他", sport_foot_short: "足球", sport_ten_short: "网球", sport_basket_short: "篮球", sport_other_short: "其他", place_new: "下新注", match_name: "比赛名称", odds: "赔率", events_on: "投注事件", total_odds: "总赔率:", conf_meter: "信心指数", ticket_name: "票据名称", stake: "投注额 (RON)", add_photo: "添加照片", no_file: "未选择文件", photo_added: "已添加照片!", risk_meter: "风险指数", risk_low: "低风险", risk_med: "中风险", risk_high: "高风险！", add_ticket: "添加票据", perf_last5: "表现 (近5场)", active_hist: "活跃与历史票据", no_tickets: "未找到票据", miza: "投注额:", payout: "派彩:", cashout_txt: "提现:", btn_win: "赢", btn_cashout: "提现", btn_loss: "输", err_odds: "名称和赔率需 > 1!", err_stake: "有效投注额!", err_event: "至少添加一个事件!", prompt_cashout: "提现金额 (RON):", alert_bal: "rGdbet: 请充值!", sport_football: "足球", sport_tennis: "网球", sport_basketball: "篮球", sport_other: "其他", income_calc: "收入计算器", calc_btn: "计算", calc_result_label: "期间利润:", calendar_title: "利润日历", create_port: "+ 创建投资组合...", prompt_name: "新名称:", prompt_bank: "初始银行 (RON):", main_bank: "主要银行", total_budget: "总预算", total_tickets: "总票数"
        },
        fr: {
          resp: "JOUEZ DE MANIÈRE RESPONSABLE", donate: "DON REVOLUT", net_profit: "Bénéfice Net", start_bal: "Solde Initial", win_rate: "Victoires", recent_form: "Forme", target_goal: "Objectif (RON)", realized: "réalisé", cashout_reset: "ENCAISSER (RESET)", filter_all: "TOUT", filter_pending: "EN ATTENTE", filter_win: "GAGNÉ", filter_loss: "PERDU", sport_all: "TOUS LES SPORTS", sport_foot: "FOOTBALL", sport_ten: "TENNIS", sport_basket: "BASKET", sport_other: "AUTRE", sport_foot_short: "Football", sport_ten_short: "Tennis", sport_basket_short: "Basket", sport_other_short: "Autre", place_new: "NOUVEAU PARI", match_name: "Nom du Match", odds: "Cote", events_on: "ÉVÉNEMENTS DU PARI", total_odds: "Cote Totale:", conf_meter: "CONFIANCE", ticket_name: "Nom du pari", stake: "Mise (RON)", add_photo: "AJOUTER PHOTO", no_file: "Aucun fichier", photo_added: "Photo ajoutée!", risk_meter: "NIVEAU DE RISQUE", risk_low: "RISQUE FAIBLE", risk_med: "RISQUE MOYEN", risk_high: "RISQUE ÉLEVÉ!", add_ticket: "AJOUTER PARI", perf_last5: "PERFORMANCES (LES 5 DERNIÈRES)", active_hist: "PARIS ACTIFS & HISTORIQUE", no_tickets: "AUCUN PARI TROUVÉ", miza: "Mise:", payout: "Gain:", cashout_txt: "Encaissement:", btn_win: "GAGNÉ", btn_cashout: "ENCAISSER", btn_loss: "PERDU", err_odds: "Nom et cote > 1 requis!", err_stake: "Mise valide requise!", err_event: "Ajoutez un événement!", prompt_cashout: "Montant à encaisser (RON):", alert_bal: "rGdbet: Ajouter du solde!", sport_football: "FOOTBALL", sport_tennis: "TENNIS", sport_basketball: "BASKET", sport_other: "AUTRE", income_calc: "CALCULATEUR REVENUS", calc_btn: "CALCULER", calc_result_label: "Bénéfice période:", calendar_title: "CALENDRIER PROFITS", create_port: "+ Créer un Portefeuille...", prompt_name: "Nom du portefeuille:", prompt_bank: "Banque initiale (RON):", main_bank: "Banque Principale", total_budget: "Budget Total", total_tickets: "Total Paris"
        },
        cs: {
          resp: "HRAJTE ZODPOVĚDNĚ", donate: "DAROVAT REVOLUT", net_profit: "Čistý zisk", start_bal: "Zůstatek", win_rate: "Výhry", recent_form: "Forma", target_goal: "Cíl (RON)", realized: "splněno", cashout_reset: "VYBRAT (RESET)", filter_all: "VŠE", filter_pending: "ČEKÁ", filter_win: "VÝHRA", filter_loss: "PROHRA", sport_all: "VŠECHNY SPORTY", sport_foot: "FOTBAL", sport_ten: "TENIS", sport_basket: "BASKETBAL", sport_other: "OSTATNÍ", sport_foot_short: "Fotbal", sport_ten_short: "Tenis", sport_basket_short: "Basketbal", sport_other_short: "Ostatní", place_new: "NOVÝ TIKET", match_name: "Název zápasu", odds: "Kurz", events_on: "UDÁLOSTI NA TIKETU", total_odds: "Celkový kurz:", conf_meter: "DŮVĚRA", ticket_name: "Název tiketu", stake: "Vklad (RON)", add_photo: "PŘIDAT FOTKU", no_file: "Žádný soubor", photo_added: "Fotka přidána!", risk_meter: "MÍRA RIZIKA", risk_low: "NÍZKÉ RIZIKO", risk_med: "STŘEDNÍ RIZIKO", risk_high: "VYSOKÉ RIZIKO!", add_ticket: "PŘIDAT TIKET", perf_last5: "VÝKON (POSLEDNÍCH 5)", active_hist: "AKTIVNÍ & HISTORIE", no_tickets: "ŽÁDNÉ TIKETY", miza: "Vklad:", payout: "Výplata:", cashout_txt: "Výběr:", btn_win: "VÝHRA", btn_cashout: "VYBRAT", btn_loss: "PROHRA", err_odds: "Název a kurz > 1!", err_stake: "Platný vklad!", err_event: "Přidejte událost!", prompt_cashout: "Částka k výběru (RON):", alert_bal: "rGdbet: Doplňte zůstatek!", sport_football: "FOTBAL", sport_tennis: "TENIS", sport_basketball: "BASKETBAL", sport_other: "OSTATNÍ", income_calc: "KALKULAČKA PŘÍJMŮ", calc_btn: "VYPOČÍTAT", calc_result_label: "Zisk za období:", calendar_title: "KALENDÁŘ ZISKŮ", create_port: "+ Vytvořit Portfolio...", prompt_name: "Název portfolia:", prompt_bank: "Počáteční banka (RON):", main_bank: "Hlavní Banka", total_budget: "Celkový rozpočet", total_tickets: "Celkem tiketů"
        },
        tr: {
          resp: "SORUMLU OYNA", donate: "REVOLUT BAĞIŞ", net_profit: "Net Kâr", start_bal: "Başlangıç Bakiyesi", win_rate: "Kazanma Oranı", recent_form: "Son Form", target_goal: "Hedef (RON)", realized: "gerçekleşti", cashout_reset: "NAKİT ÇIKIŞI (SIFIRLA)", filter_all: "HEPSİ", filter_pending: "BEKLEYEN", filter_win: "KAZANAN", filter_loss: "KAYBEDEN", sport_all: "TÜM SPORLAR", sport_foot: "FUTBOL", sport_ten: "TENİS", sport_basket: "BASKETBOL", sport_other: "DİĞER", sport_foot_short: "Futbol", sport_ten_short: "Tenis", sport_basket_short: "Basketbol", sport_other_short: "Diğer", place_new: "YENİ KUPON OLUŞTUR", match_name: "Maç / Seçim Adı", odds: "Oran", events_on: "KUPONDAKİ MAÇLAR", total_odds: "Toplam Oran:", conf_meter: "GÜVEN SEVİYESİ", ticket_name: "Kupon adı (isteğe bağlı)", stake: "Bahis Miktarı (RON)", add_photo: "FOTOĞRAF EKLE", no_file: "Dosya seçilmedi", photo_added: "Fotoğraf eklendi!", risk_meter: "RİSK SEVİYESİ", risk_low: "DÜŞÜK RİSK", risk_med: "ORTA RİSK", risk_high: "YÜKSEK RİSK! DİKKAT", add_ticket: "KUPONU EKLE", perf_last5: "PERFORMANS (SON 5)", active_hist: "AKTİF VE GEÇMİŞ KUPONLAR", no_tickets: "KUPON BULUNAMADI", miza: "Miktar:", payout: "Kazanç:", cashout_txt: "Nakit Çıkışı:", btn_win: "KAZANDI", btn_cashout: "NAKİT ÇIKIŞI", btn_loss: "KAYBETTİ", err_odds: "Geçerli isim ve oran > 1 gerekli!", err_stake: "Geçerli miktar girin!", err_event: "En az bir maç ekleyin!", prompt_cashout: "Nakit çıkış miktarı (RON):", alert_bal: "rGdbet: Bakiye ekle!", sport_football: "FUTBOL", sport_tennis: "TENİS", sport_basketball: "BASKETBOL", sport_other: "DİĞER", income_calc: "GELİR HESAPLAYICI", calc_btn: "HESAPLA", calc_result_label: "Dönem kârı:", calendar_title: "KÂR TAKVİMİ", create_port: "+ Portföy Oluştur...", prompt_name: "Portföy adı:", prompt_bank: "Başlangıç bankası (RON):", main_bank: "Ana Banka", total_budget: "Toplam Bütçe", total_tickets: "Toplam Kupon"
        }
      };

      let bets = JSON.parse(localStorage.getItem('rgb_bets')) || [];

/* ── Performance: debounced render ── */
let _renderPending = false;
function scheduleRender() {
  // Asigurăm vizibilitatea profilului dacă suntem logați (prin Firebase)
  const topUserBtn = document.getElementById('topUserBtn');
  if (topUserBtn) topUserBtn.style.display = 'flex';

  if (_renderPending) return;
  _renderPending = true;
  requestAnimationFrame(() => {
    _renderPending = false;
    render();
  });
}
      let portfolios = JSON.parse(localStorage.getItem('rgb_portfolios')) || [];
      let currentPortfolioId = localStorage.getItem('rgb_current_portfolio') || 'default';

      let ticketEvents = [];
      let currentSport = 'football';
      let currentConfidence = 1;
      let kellyProbability = 0.55; // probabilitate estimata de utilizator pentru Kelly, INDEPENDENTA de stelele de incredere
      let chartInstance = null;
      let sportChartInstance = null;
      let currentFilter = 'all';
      let currentSportFilter = 'all';
      let currentWallpaperMode = localStorage.getItem('rgb_wallpaper_mode') || 'standard';
      let base64Photo = "";
      let calDate = new Date();
      let currentPagPage = 1;
      const itemsPerPage = 5;

      /* --- LANGUAGE & CURRENCY LOGIC --- */
      let currentLang = localStorage.getItem('rgb_lang') || 'en';
      const currencyMap = {
        en: 'USD',
        ro: 'RON',
        it: 'EUR',
        es: 'EUR',
        de: 'EUR',
        pt: 'EUR',
        ru: 'RUB',
        bg: 'BGN',
        zh: 'CNY',
        fr: 'EUR',
        cs: 'CZK',
        tr: 'TRY'
      };

      function getCurrency() {
        const manual = localStorage.getItem('rgb_manual_currency');
        if (manual) return manual;
        return currencyMap[currentLang] || 'USD';
      }

      function changeLanguage(lang) {
        currentLang = lang;
        localStorage.setItem('rgb_lang', lang);
        applyTranslations();
        initPortfolios();
        render();
        if (typeof buildProfilePage === 'function') buildProfilePage(true);
      }

      function setManualCurrency(curr) {
        if (curr === 'auto') {
          localStorage.removeItem('rgb_manual_currency');
        } else {
          localStorage.setItem('rgb_manual_currency', curr);
        }
        applyTranslations();
        initPortfolios();
        render();
        if (typeof buildProfilePage === 'function') buildProfilePage(true);
      }

      function t(key) {
        let text = translations['en'][key] || key;
        if (translations[currentLang] && translations[currentLang][key]) {
          text = translations[currentLang][key];
        }

        // Înlocuim dinamic (RON) cu moneda curentă în traduceri
        const curr = getCurrency();
        return text.replace(/\(RON\)/g, `(${curr})`).replace(/RON/g, curr);
      }

      /* ── 3D TILT EFFECT FOR OBSIDIAN CARDS ── */
      document.addEventListener('mousemove', (e) => {
        const cards = document.querySelectorAll('.stat-card, .form-card, .bet-item, .bankroll-health-card, .target-card');
        cards.forEach(card => {
          if (card.id === 'add-ticket-card' || card.id === 'daily-quests-container') {
            card.style.transform = '';
            card.style.zIndex = "";
            return;
          }
          const rect = card.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;

          if (x > 0 && x < rect.width && y > 0 && y < rect.height) {
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            const rotateX = (y - centerY) / 12;
            const rotateY = (centerX - x) / 12;

            card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
            card.style.zIndex = "100";
          } else {
            card.style.transform = '';
            card.style.zIndex = "";
          }
        });
      });

      /* Pentru mobil - atingere */
      document.addEventListener('touchmove', (e) => {
        const touch = e.touches[0];
        const cards = document.querySelectorAll('.stat-card, .form-card, .bet-item, .bankroll-health-card, .target-card');
        cards.forEach(card => {
          if (card.id === 'add-ticket-card' || card.id === 'daily-quests-container') {
            card.style.transform = '';
            card.style.zIndex = "";
            return;
          }
          const rect = card.getBoundingClientRect();
          const x = touch.clientX - rect.left;
          const y = touch.clientY - rect.top;

          if (x > 0 && x < rect.width && y > 0 && y < rect.height) {
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            const rotateX = (y - centerY) / 12;
            const rotateY = (centerX - x) / 12;

            card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
          } else {
            card.style.transform = '';
          }
        });
      }, { passive: true });

      function applyTranslations() {
        document.querySelectorAll('[data-i18n]').forEach(el => {
          const key = el.getAttribute('data-i18n');
          el.textContent = t(key);
        });
        document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
          const key = el.getAttribute('data-i18n-placeholder');
          el.setAttribute('placeholder', t(key));
        });
      }

      /* --- MIGRATE TO PORTFOLIOS & INIT --- */
      function runDataMigrations() {
        if (portfolios.length === 0) {
          let oldBudget = parseFloat(localStorage.getItem('rgb_initial_budget')) || 1000;
          let oldOffset = parseFloat(localStorage.getItem('rgb_profit_offset')) || 0;
          let oldTarget = parseFloat(localStorage.getItem('rgb_target')) || 1000;
          portfolios.push({
            id: 'default',
            name: currentLang === 'ro' ? 'Banca Principală' : 'Main Bank',
            budget: oldBudget,
            offset: oldOffset,
            target: oldTarget
          });
          localStorage.setItem('rgb_portfolios', JSON.stringify(portfolios));
        }

        let betsMigrated = false;
        bets.forEach(b => {
          if (!b.portfolioId) {
            b.portfolioId = 'default';
            betsMigrated = true;
          }
        });
        if (betsMigrated) {
          localStorage.setItem('rgb_bets', JSON.stringify(bets));
        }
      }

      function initPortfolios() {
        const sel = document.getElementById('portfolioSelect');
        sel.innerHTML = '';
        portfolios.forEach(p => {
          let opt = document.createElement('option');
          opt.value = p.id;
          opt.textContent = p.name;
          sel.appendChild(opt);
        });
        let createOpt = document.createElement('option');
        createOpt.value = 'new';
        createOpt.textContent = t('create_port');
        sel.appendChild(createOpt);

        sel.value = currentPortfolioId;
        applyPortfolioData();
      }

      function handlePortfolioChange(val) {
        if (val === 'new') {
          createNewPortfolio();
        } else {
          currentPortfolioId = val;
          localStorage.setItem('rgb_current_portfolio', currentPortfolioId);
          applyPortfolioData();
          render();
          updateKellyRecommendation();
        }
      }

      function createNewPortfolio() {
        let pName = prompt(t('prompt_name') || "Nume portofoliu nou:");
        if (!pName) {
          document.getElementById('portfolioSelect').value = currentPortfolioId;
          return;
        }
        let pBankStr = prompt(t('prompt_bank') || "Bancă inițială (RON):", "1000");
        let pBank = parseFloat(pBankStr) || 1000;

        let newId = 'port_' + Date.now();
        portfolios.push({
          id: newId,
          name: pName,
          budget: pBank,
          offset: 0,
          target: pBank * 2
        });
        localStorage.setItem('rgb_portfolios', JSON.stringify(portfolios));

        currentPortfolioId = newId;
        localStorage.setItem('rgb_current_portfolio', currentPortfolioId);

        initPortfolios();
        render();
      }

      function applyPortfolioData() {
        let p = portfolios.find(x => x.id === currentPortfolioId) || portfolios[0];
        document.getElementById('budget-input').value = p.budget;
        document.getElementById('target-input').value = p.target;
      }

      function getCurrentBets() {
        if (!currentPortfolioId || currentPortfolioId === 'default') {
          return bets.filter(b => !b.portfolioId || b.portfolioId === 'default');
        }
        return bets.filter(b => b.portfolioId === currentPortfolioId);
      }

      function toggleCustomWallpaper() {
        const body = document.body;
        const icon = document.getElementById('themeIcon');

        if (body.classList.contains('mode-standard')) {
          body.classList.remove('mode-standard');
          body.classList.add('mode-gold');
          if(icon) icon.className = 'fa-solid fa-sun';
        } else if (body.classList.contains('mode-gold')) {
          body.classList.remove('mode-gold');
          body.classList.add('legend-mode');
          if(icon) icon.className = 'fa-solid fa-star';
        } else {
          body.classList.remove('legend-mode');
          body.classList.add('mode-standard');
          if(icon) icon.className = 'fa-solid fa-moon';
        }

        let newMode = 'standard';
        if(body.classList.contains('mode-gold')) newMode = 'gold';
        if(body.classList.contains('legend-mode')) newMode = 'legend';

        localStorage.setItem('rgb_wallpaper_mode', newMode);
        updateChart();
      }

      window.addEventListener('DOMContentLoaded', () => {
        document.body.classList.remove('mode-standard', 'mode-gold', 'legend-mode');
        let modeToApply = currentWallpaperMode;
        if(modeToApply === 'legend') modeToApply = 'legend-mode';
        else modeToApply = 'mode-' + modeToApply;

        document.body.classList.add(modeToApply);

        const icon = document.getElementById('themeIcon');
        if(icon)
        {
          if(currentWallpaperMode === 'gold') icon.className = 'fa-solid fa-sun';
          else if(currentWallpaperMode === 'legend') icon.className = 'fa-solid fa-star';
          else icon.className = 'fa-solid fa-moon';
        }

        const today = new Date().toISOString().split('T')[0];

        const dateInput = document.getElementById('ticket-date');
        if (dateInput) {
          dateInput.value = today;
        }

        const calcStart = document.getElementById('calc-start');
        const calcEnd = document.getElementById('calc-end');
        if (calcStart) calcStart.value = today;
        if (calcEnd) calcEnd.value = today;

        document.getElementById('langSelect').value = currentLang;

        runDataMigrations();
        initPortfolios();
        applyTranslations();
        if (typeof initQuests === 'function') initQuests();
        render();
      });

      function setSport(sport, el) {
        document.querySelectorAll('[data-sport-pill]').forEach(b => b.classList.remove('sel'));
        el.classList.add('sel');
        currentSport = sport;
      }

      function setConfidence(val) {
        currentConfidence = val;
        const stars = document.querySelectorAll('.star-btn');
        stars.forEach((s, idx) => {
          if(idx < val) s.classList.add('active');
          else s.classList.remove('active');
        });
      }

      function handlePhotoSelect(input) {
        const file = input.files[0];
        if (file) {
          const reader = new FileReader();
          reader.onload = function(e) {
            base64Photo = e.target.result;
            const statusEl = document.getElementById('photo-status');
            statusEl.setAttribute('data-i18n', 'photo_added');
            statusEl.textContent = t('photo_added');
          };
          reader.readAsDataURL(file);
        }
      }

      function addEventToTicket() {
        const nameInput = document.getElementById('event-name');
        const leagueInput = document.getElementById('event-league');
        const oddsInput = document.getElementById('event-odds');
        const marketInput = document.getElementById('event-market');
        const timeInput = document.getElementById('event-time');

        const name = nameInput.value.trim();
        const league = leagueInput?.value.trim() || "";
        const market = marketInput.value.trim();
        const odds = parseFloat(oddsInput.value);
        const time = timeInput?.value || "";

        if(!name || !odds || odds <= 1) {
          alert(t('err_odds'));
          return;
        }

        ticketEvents.push({
          name: name,
          league: league,
          market: market || (currentLang === 'ro' ? 'Pronostic' : 'Market'),
          odds: odds,
          time: time
        });

        // Nu resetam numele meciului pentru a facilita adaugarea mai multor piete (BetBuilder)
        oddsInput.value = '';
        marketInput.value = '';
        if (timeInput) timeInput.value = '';
        renderEvents();
      }

      function renderEvents() {
        const list = document.getElementById('ticketEventsList');
        const bar = document.getElementById('totalOddsBar');
        const val = document.getElementById('totalOddsVal');
        const transLabel = document.getElementById('trans_total_odds');
        if (transLabel) transLabel.textContent = t('total_odds');
        if(list) list.innerHTML = '';

        let totalOdds = 1;

        // Grouping events by match name for visual display
        const grouped = {};
        ticketEvents.forEach((ev, idx) => {
          if (!grouped[ev.name]) grouped[ev.name] = [];
          grouped[ev.name].push({ ...ev, originalIndex: idx });
        });

        Object.keys(grouped).forEach(matchName => {
          const events = grouped[matchName];
          const leagueName = events[0].league || "";
          const eventTime = events[0].time || "";
          let groupHtml = `<div class="event-group">
            <div class="event-group-header">
              <div style="display:flex; justify-content:space-between; align-items:center;">
                <span>${eventTime ? `<span style="color:var(--nb); margin-right:6px;">${eventTime}</span>` : ''}⚽ ${matchName}</span>
                ${leagueName ? `<span style="font-size:8px; opacity:0.6; text-transform:uppercase;">${leagueName}</span>` : ''}
              </div>
            </div>`;

          events.forEach(ev => {
            totalOdds *= ev.odds;
            groupHtml += `
              <div class="event-item bb-item">
                <span class="event-item-name">🔸 ${ev.market}</span>
                <span class="event-item-odds">@${ev.odds.toFixed(2)}</span>
                <button class="event-del" onclick="removeEvent(${ev.originalIndex})"><i class="fa-solid fa-trash"></i></button>
              </div>
            `;
          });

          groupHtml += `</div>`;
          if(list) list.innerHTML += groupHtml;
        });

        if(ticketEvents.length > 0) {
          if(bar) bar.style.display = 'flex';
          if(val) val.textContent = totalOdds.toFixed(2);
        } else {
          if(bar) bar.style.display = 'none';
          if(val) val.textContent = '1.00';
        }
        updateRiskMeter();
        updateKellyRecommendation();
      }

      function removeEvent(idx) {
        ticketEvents.splice(idx, 1);
        renderEvents();
      }

      /* ═══════════════════════════════════════════════
         GESTIONAREA MIZELOR ÎN FUNCȚIE DE COTĂ
         (Odds-Based Stake Management / Inverse Staking System)

         Logica inversă: cu cât cota e mai mare, cu atât miza
         recomandată scade. Este un instrument de disciplină
         pentru limitarea expunerii la risc atunci când
         probabilitatea statistică de succes e mai mică
         (cote mari = risc mai mare = miză mai mică).

         Formulă: Miză% = K / CotăTotală
         K = constantă de calibrare (vezi INVERSE_STAKE_K)

         NOTĂ: Probabilitatea estimată (slider) e păstrată ca
         informație afișată și salvată pe bilet, dar NU mai
         intră în formula de calcul a mizei — sistemul de mize
         inverse se bazează STRICT pe cotă, fiind o metodă mai
         conservatoare și mai ușor de controlat emoțional decât
         un model probabilistic precum Criteriul Kelly.
      ═══════════════════════════════════════════════ */
      const INVERSE_STAKE_K   = 7;   // constanta de calibrare a formulei Miza% = K / Cota
      const INVERSE_STAKE_CAP = 10;  // plafon de siguranta: maxim 10% din banca, indiferent de cota

      function onKellyProbChange(val) {
        kellyProbability = (parseFloat(val) || 55) / 100;
        const probValEl = document.getElementById('kellyProbVal');
        if (probValEl) probValEl.textContent = val + '%';
        // Probabilitatea NU influenteaza formula de miza (sistem bazat strict pe cota),
        // dar pastram bara vizibila si actualizata pentru referinta utilizatorului.
      }

      function getCurrentBankValue() {
        let p = portfolios.find(x => x.id === currentPortfolioId);
        let initialBudget = p ? (parseFloat(p.budget) || 0) : 1000;
        let profitOffset = p ? (parseFloat(p.offset) || 0) : 0;
        let netProfit = 0;
        getCurrentBets().forEach(b => {
          if (b.status === 'win') netProfit += (b.stake * b.odds) - b.stake;
          else if (b.status === 'loss') netProfit -= b.stake;
          else if (b.status === 'cashout') netProfit += (b.cashoutAmount - b.stake);
        });
        let displayedProfit = netProfit - profitOffset;
        return initialBudget + displayedProfit;
      }

      function calculateInverseStakePct(totalOdds) {
        if (totalOdds <= 1) return 0;
        const pct = INVERSE_STAKE_K / totalOdds; // logica inversa: cota mare -> procent mic
        return Math.min(pct, INVERSE_STAKE_CAP);
      }

      function updateKellyRecommendation() {
        const wrap     = document.getElementById('kellyBox');
        const box      = document.getElementById('kellyReco');
        const txtSpan  = document.getElementById('kellyRecoText');
        if (!wrap || !box || !txtSpan) return;

        // Cota Totala se calculeaza STRICT din evenimentele deja confirmate pe bilet
        // (ticketEvents) — adica suma/produsul tuturor evenimentelor adaugate, nu doar primul.
        let totalOdds = 1;
        ticketEvents.forEach(e => totalOdds *= e.odds);

        // Avem nevoie de minim un eveniment confirmat pe bilet pentru a recomanda o miza
        if (ticketEvents.length === 0 || totalOdds <= 1) {
          wrap.style.display = 'none';
          return;
        }

        wrap.style.display = 'block';
        box.classList.remove('kelly-warn');

        const bank = getCurrentBankValue();
        const recoPct = calculateInverseStakePct(totalOdds);
        const recoAmount = bank > 0 ? bank * (recoPct / 100) : 0;
        const recoAmountRounded = Math.round(recoAmount * 100) / 100;

        // IMPORTANT: pastram id="kellyRecoPct" pe noul <strong>, altfel elementul
        // dispare din DOM la fiecare re-randare si urmatoarele apeluri ies prematur.
        txtSpan.innerHTML = `rGdbet recomandă o miză de <strong id="kellyRecoPct">${recoPct.toFixed(1)}%</strong> din Banca Curentă <span style="opacity:.7;font-weight:600;">(~${recoAmountRounded.toFixed(0)} RON)</span>`;
        // NOTA: recomandarea este STRICT informativa — NU completeaza automat campul Miza.
      }

      function updateRiskMeter() {
        const stake = parseFloat(document.getElementById('stake').value) || 0;
        let totalOdds = 1;
        ticketEvents.forEach(e => totalOdds *= e.odds);
        let score = (stake * 0.1) + (totalOdds * 2);
        let fill = document.getElementById('riskFill');
        let label = document.getElementById('riskValue');
        let pct = Math.min(Math.max(score * 2, 5), 100);
        if(fill) fill.style.width = pct + '%';
        if(label) {
          if(pct < 35) {
            fill.style.background = 'var(--ng)';
            label.setAttribute('data-i18n', 'risk_low');
            label.textContent = t('risk_low');
          } else if(pct < 70) {
            fill.style.background = 'var(--nb)';
            label.setAttribute('data-i18n', 'risk_med');
            label.textContent = t('risk_med');
          } else {
            fill.style.background = 'var(--danger)';
            label.setAttribute('data-i18n', 'risk_high');
            label.textContent = t('risk_high');
          }
        }
      }

      function playSuccessSound() {
        try {
          const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
          const oscillator = audioCtx.createOscillator();
          const gainNode = audioCtx.createGain();

          oscillator.type = 'sine';
          oscillator.frequency.setValueAtTime(880, audioCtx.currentTime); // A5
          oscillator.frequency.exponentialRampToValueAtTime(440, audioCtx.currentTime + 0.1); // Slide down to A4

          gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime);
          gainNode.gain.exponentialRampToValueAtTime(0.01, audioCtx.currentTime + 0.2);

          oscillator.connect(gainNode);
          gainNode.connect(audioCtx.destination);

          oscillator.start();
          oscillator.stop(audioCtx.currentTime + 0.2);
        } catch (e) { console.log("Audio API error", e); }
      }

      function requestPlaceTicket() {
        const stake = parseFloat(document.getElementById('stake').value);
        if(!stake || stake <= 0) {
          alert(t('err_stake'));
          return;
        }

        // Dacă lista de evenimente e goală, dar câmpurile de sus sunt completate, le adăugăm automat ca un singur eveniment
        if(ticketEvents.length === 0) {
          const nameInput = document.getElementById('event-name');
          const oddsInput = document.getElementById('event-odds');
          const marketInput = document.getElementById('event-market');
          const name = nameInput.value.trim();
          const odds = parseFloat(oddsInput.value);
          const market = marketInput.value.trim();

          if(name && odds && odds > 1) {
            ticketEvents.push({
              name: name,
              market: market || (currentLang === 'ro' ? 'Pronostic' : 'Market'),
              odds: odds
            });
          } else {
            alert(t('err_event'));
            return;
          }
        }

        let totalOdds = 1;
        ticketEvents.forEach(e => totalOdds *= e.odds);

        // Numele biletului va fi numele primului eveniment (sau generic)
        const ticketName = (ticketEvents.length > 0) ? ticketEvents[0].name : `Ticket #${getCurrentBets().length + 1}`;

        // Populăm sumarul din modalul de confirmare
        document.getElementById('ctsName').textContent = ticketName;
        document.getElementById('ctsEvents').textContent = ticketEvents.length + (ticketEvents.length === 1 ? ' eveniment' : ' evenimente');
        document.getElementById('ctsOdds').textContent = totalOdds.toFixed(2);
        document.getElementById('ctsStake').textContent = stake.toFixed(2) + ' RON';
        document.getElementById('ctsPayout').textContent = (stake * totalOdds).toFixed(2) + ' RON';

        const overlay = document.getElementById('confirm-ticket-modal');
        if (overlay) overlay.style.display = 'flex';
      }

      function cancelPlaceTicket() {
        const overlay = document.getElementById('confirm-ticket-modal');
        if (overlay) overlay.style.display = 'none';
      }

      function confirmPlaceTicket() {
        const overlay = document.getElementById('confirm-ticket-modal');
        if (overlay) overlay.style.display = 'none';

        const btn = document.querySelector('.main-btn');
        const stake = parseFloat(document.getElementById('stake').value);
        if(!stake || stake <= 0) {
          alert(t('err_stake'));
          return;
        }

        if(ticketEvents.length === 0) {
          alert(t('err_event'));
          return;
        }

        // Efecte vizuale, sonore și haptice
        if (btn) {
          btn.classList.add('btn-flash');
          setTimeout(() => btn.classList.remove('btn-flash'), 400);
        }
        playSuccessSound();
        if (window.Android && typeof Android.vibrate === 'function') {
          Android.vibrate(130); // 50ms vibrație scurtă
        }

        let totalOdds = 1;
        ticketEvents.forEach(e => totalOdds *= e.odds);
        const ticketName = (ticketEvents.length > 0) ? ticketEvents[0].name : `Ticket #${getCurrentBets().length + 1}`;
        const dateVal = document.getElementById('ticket-date').value;
        const newBet = {
          id: Date.now(),
          portfolioId: currentPortfolioId,
          name: ticketName,
          sport: currentSport,
          confidence: currentConfidence,
          stake: stake,
          odds: totalOdds,
          date: dateVal || new Date().toISOString().split('T')[0],
          events: [...ticketEvents],
          status: 'pending',
          photo: base64Photo,
          kellyProbability: kellyProbability // probabilitatea estimata de succes (slider Kelly), salvata pe bilet
        };
        bets.push(newBet);
        localStorage.setItem('rgb_bets', JSON.stringify(bets));

        // Recompensă XP (v8.5 Platinum - Logică Anti-Double)
        const baseXP = typeof calculateTicketXP === 'function' ? calculateTicketXP(newBet) : 50;
        newBet.rewardedXP = baseXP;
        if (typeof addXP === 'function') addXP(baseXP);

        // Update Quest Progress
        if (typeof updateQuestProgress === 'function') updateQuestProgress('place_bet');

        document.getElementById('event-name').value = '';
        document.getElementById('event-league').value = '';
        document.getElementById('stake').value = '';
        document.getElementById('ticket-photo').value = '';
        document.getElementById('ticket-date').value = new Date().toISOString().split('T')[0];

        // Resetare câmpuri eveniment
        document.getElementById('event-name').value = '';
        document.getElementById('event-market').value = '';
        document.getElementById('event-odds').value = '';

        const statusEl = document.getElementById('photo-status');
        if(statusEl) {
          statusEl.setAttribute('data-i18n', 'no_file');
          statusEl.textContent = t('no_file');
        }

        base64Photo = "";
        ticketEvents = [];
        kellyProbability = 0.55;
        const kellySlider = document.getElementById('kellyProbSlider');
        const kellyProbVal = document.getElementById('kellyProbVal');
        if (kellySlider) kellySlider.value = 55;
        if (kellyProbVal) kellyProbVal.textContent = '55%';
        renderEvents();
        render();
      }

      /* ── LOGICĂ INTEGRIȚATE XP (v8.7 Platinum) ── */
      /**
       * Sincronizează XP-ul unui bilet. Calculează cât XP ar trebui să aibă
       * și ajustează balanța utilizatorului cu diferența (Delta).
       */
      function syncTicketXP(bet, forceStatus = null) {
        if (typeof addXP !== 'function' || typeof calculateTicketXP !== 'function') return;

        const targetStatus = forceStatus || bet.status;
        const tempBet = { ...bet, status: targetStatus };

        // Calculăm cât ar trebui să valoreze biletul ACUM
        const currentValue = calculateTicketXP(tempBet);

        // Cât am oferit deja? (Dacă e bilet vechi fără rewardedXP, îl calculăm pe loc)
        if (bet.rewardedXP === undefined) bet.rewardedXP = calculateTicketXP(bet);
        const alreadyAwarded = bet.rewardedXP;

        const delta = currentValue - alreadyAwarded;

        if (delta !== 0) {
          // Ajustăm XP-ul total al contului
          addXP(delta, true);
          // Actualizăm "memoria" biletului
          bet.rewardedXP = currentValue;
        }
      }

      function changeStatus(id, newStatus) {
        const bet = bets.find(b => b.id === id);
        if(bet) {
          const oldStatus = bet.status;
          if (newStatus === oldStatus) return;

          syncTicketXP(bet, newStatus);
          bet.status = newStatus;

          localStorage.setItem('rgb_bets', JSON.stringify(bets));

          if (newStatus === 'win' && bet.odds >= 2.0) {
            if (typeof updateQuestProgress === 'function') updateQuestProgress('high_odds');
          }

          if((newStatus === 'win' || (newStatus === 'cashout' && bet.cashoutAmount > 0)) && typeof confetti === 'function') {
            triggerWinConfetti();
          }
          scheduleRender();
          if (window.streakMode) setTimeout(window.streakMode.detect, 150);
        }
      }

      function triggerWinConfetti() {
        const duration = 3 * 2000;
        const animationEnd = Date.now() + duration;
        const defaults = { startVelocity: 30, spread: 360, ticks: 99, zIndex: 0 };

        function randomInRange(min, max) {
          return Math.random() * (max - min) + min;
        }

        const interval = setInterval(function() {
          const timeLeft = animationEnd - Date.now();

          if (timeLeft <= 0) {
            return clearInterval(interval);
          }

          const particleCount = 50 * (timeLeft / duration);
          confetti(Object.assign({}, defaults, {
            particleCount,
            origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 },
            colors: ['#ffcc00', '#00ff88', '#00c8ff']
          }));
          confetti(Object.assign({}, defaults, {
            particleCount,
            origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 },
            colors: ['#ffcc00', '#00ff88', '#00c8ff']
          }));
        }, 250);
      }

      function cashoutBet(id) {
        const amount = prompt(t('prompt_cashout'));
        if (amount !== null && !isNaN(amount) && amount > 0) {
          const bet = bets.find(b => b.id === id);
          if (bet) {
            syncTicketXP(bet, 'cashout');
            bet.status = 'cashout';
            bet.cashoutAmount = parseFloat(amount);
            localStorage.setItem('rgb_bets', JSON.stringify(bets));
            if(typeof confetti === 'function') {
              triggerWinConfetti();
            }
            render();
          }
        }
      }

      function makeStatusPending(id) {
        const bet = bets.find(b => b.id === id);
        if(bet) {
          syncTicketXP(bet, 'pending');
          bet.status = 'pending';
          if(bet.cashoutAmount) delete bet.cashoutAmount;
          localStorage.setItem('rgb_bets', JSON.stringify(bets));
          render();
        }
      }

      function viewFullImage(base64Str) {
        const win = window.open();
        win.document.write(`<iframe src="${base64Str}" frameborder="0" style="border:0; top:0px; left:0px; bottom:0px; right:0px; width:100%; height:100%; background-color: #080b10;" allowfullscreen></iframe>`);
      }

      function globalCashoutReset() {
        alert(t('alert_bal'));
        let netProfit = 0;
        getCurrentBets().forEach(b => {
          if(b.status === 'win') {
            netProfit += (b.stake * b.odds) - b.stake;
          } else if(b.status === 'loss') {
            netProfit -= b.stake;
          } else if(b.status === 'cashout') {
            netProfit += (b.cashoutAmount - b.stake);
          }
        });
        let p = portfolios.find(x => x.id === currentPortfolioId);
        if(p) {
          p.offset = netProfit;
          p.budget = 0;
          localStorage.setItem('rgb_portfolios', JSON.stringify(portfolios));
        }
        applyPortfolioData();
        render();
      }

      /* --- ELIMINARE POSIBILITATE STERGERE BILETE (v9.0 Elite) --- */
      /* Biletele sunt permanente pentru integritatea Rangului și Istoricului */

      function setFilter(f, btn) {
        document.querySelectorAll('[data-filter]').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        currentFilter = f;
        currentPagPage = 1; // Reset la prima pagină
        render();
      }

      function setSportFilter(s, btn) {
        document.querySelectorAll('[data-sport]').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        currentSportFilter = s;
        currentPagPage = 1; // Reset la prima pagină
        render();
      }

      /* --- CALCULATOR DE VENIT FUNCȚIONALITATE --- */
      function calculatePeriodIncome() {
        const startDateVal = document.getElementById('calc-start').value;
        const endDateVal = document.getElementById('calc-end').value;

        if (!startDateVal || !endDateVal) {
          alert(currentLang === 'ro' ? 'Selectați ambele date!' : 'Please select both dates!');
          return;
        }

        const start = new Date(startDateVal);
        const end = new Date(endDateVal);
        start.setHours(0, 0, 0, 0);
        end.setHours(23, 59, 59, 999);

        let periodProfit = 0;
        getCurrentBets().forEach(b => {
          if (!b.date) return;
          const betDate = new Date(b.date);

          if (betDate >= start && betDate <= end) {
            if(b.status === 'win') {
              periodProfit += (b.stake * b.odds) - b.stake;
            } else if(b.status === 'loss') {
              periodProfit -= b.stake;
            } else if(b.status === 'cashout') {
              periodProfit += (b.cashoutAmount - b.stake);
            }
          }
        });
        const resultBar = document.getElementById('calcResultBar');
        const resultVal = document.getElementById('calcResultVal');

        resultBar.style.display = 'flex';
        if (periodProfit > 0) {
          resultVal.style.color = 'var(--ng)';
          resultVal.textContent = '+' + periodProfit.toFixed(2) + ' RON';
        } else if (periodProfit < 0) {
          resultVal.style.color = 'var(--danger)';
          resultVal.textContent = periodProfit.toFixed(2) + ' RON';
        } else {
          resultVal.style.color = 'var(--text)';
          resultVal.textContent = '0.00 RON';
        }
      }

      /* --- CALENDAR DE PROFIT FUNCȚIONALITATE --- */
      function changeCalMonth(dir) {
        calDate.setMonth(calDate.getMonth() + dir);
        renderProfitCalendar();
      }

      function renderProfitCalendar() {
        const grid = document.getElementById('cal-grid');
        const monthLabel = document.getElementById('cal-month-year');
        if (!grid || !monthLabel) return;

        const year = calDate.getFullYear();
        const month = calDate.getMonth();
        const monthName = calDate.toLocaleString(currentLang === 'ro' ? 'ro-RO' : 'en-GB', { month: 'long', year: 'numeric' });
        monthLabel.textContent = monthName.toUpperCase();
        let dailyProfits = {};
        getCurrentBets().forEach(b => {
          if (b.status === 'win' || b.status === 'loss' || b.status === 'cashout') {
            const bDate = new Date(b.date || parseInt(b.id));
            if (bDate.getFullYear() === year && bDate.getMonth() === month) {
              const day = bDate.getDate();
              let profit = 0;
              if (b.status === 'win') profit = (b.stake * b.odds) - b.stake;
              else if (b.status === 'loss') profit = -b.stake;
              else if (b.status === 'cashout') profit = b.cashoutAmount - b.stake;

              dailyProfits[day] = (dailyProfits[day] || 0) + profit;
            }
          }
        });
        grid.innerHTML = '';
        const firstDay = new Date(year, month, 1).getDay();
        const daysInMonth = new Date(year, month + 1, 0).getDate();
        let startOffset = firstDay === 0 ? 6 : firstDay - 1;
        for (let i = 0; i < startOffset; i++) {
          grid.innerHTML += `<div class="cal-cell empty"></div>`;
        }

        for (let d = 1; d <= daysInMonth; d++) {
          let pnl = dailyProfits[d];
          let cellClass = 'cal-cell';
          let valHtml = '';

          if (pnl !== undefined) {
            if (pnl > 0) {
              cellClass += ' cal-profit';
              valHtml = `<div class="cal-val">+${pnl.toFixed(0)}</div>`;
            } else if (pnl < 0) {
              cellClass += ' cal-loss';
              valHtml = `<div class="cal-val">${pnl.toFixed(0)}</div>`;
            } else {
              cellClass += ' cal-neutral';
              valHtml = `<div class="cal-val">0</div>`;
            }
          }

          grid.innerHTML += `
            <div class="${cellClass}">
              ${d}
              ${valHtml}
            </div>
          `;
        }
      }

      function renderStreakFlames() {
        const streakBar = document.getElementById('streakBar');
        if (!streakBar) return;

        const recentBets = getCurrentBets().filter(b => b.status === 'win' || b.status === 'loss' || b.status === 'cashout').slice(-5);
        const wins = recentBets.filter(b => b.status === 'win' || b.status === 'cashout').length;
        const percentage = recentBets.length > 0 ? (wins / recentBets.length) * 100 : 0;

        streakBar.innerHTML = `
          <div class="momentum-container">
            <div class="momentum-label">
              <span>${t('perf_last5')}</span>
              <span>${percentage.toFixed(0)}%</span>
            </div>
            <div class="momentum-track">
              <div class="momentum-fill" style="width: ${percentage}%"></div>
            </div>
          </div>
        `;
      }

      function handleDeposit(val) {
        let p = portfolios.find(x => x.id === currentPortfolioId);
        if(p) {
          p.budget = parseFloat(val) || 0;
          localStorage.setItem('rgb_portfolios', JSON.stringify(portfolios));
          renderStatsOnly();
          updateChart();
        }
      }

      function updateTargetMeter() {
        const netProfitEl = document.getElementById('total-p');
        if (!netProfitEl) return;
        const netProfitStr = netProfitEl.textContent.replace(/[+]/g, '').trim();
        const netProfit = parseFloat(netProfitStr) || 0;
        const targetSum = parseFloat(document.getElementById('target-input').value) || 1;
        let p = portfolios.find(x => x.id === currentPortfolioId);
        if(p) {
          p.target = targetSum;
          localStorage.setItem('rgb_portfolios', JSON.stringify(portfolios));
        }

        let percentage = (netProfit / targetSum) * 100;
        const displayPercent = Math.min(Math.max(percentage, 0), 100);

        const targetFill = document.getElementById('targetFill');
        const targetText = document.getElementById('targetText');
        if (targetFill) {
          targetFill.style.width = displayPercent + '%';
          const hue = displayPercent * 1.2;
          const color = `hsl(${hue}, 100%, 50%)`;
          targetFill.style.setProperty('--target-glow', color);
          targetFill.style.boxShadow = `0 0 15px ${color}, inset 0 0 10px rgba(255,255,255,0.3)`;
        }
        if (targetText) targetText.textContent = percentage.toFixed(1) + '% ' + t('realized');
      }

      /* ═══ ACTUALIZAREA STATISTICILOR (NUMAR BILETE) ═══ */
      /* ── ANIMATE PROFIT ROLL (REAL SLOT MACHINE FX v22.0) ── */
      let _isRolling = false;
      function animateProfitRoll() {
        if (_isRolling) return;
        const totalP = document.getElementById('total-p');
        if (!totalP) return;

        _isRolling = true;
        const finalValue = totalP.textContent; // e.g. "+123.45"
        const isPos = finalValue.startsWith('+');

        // Dezactivăm hover-ul și adăugăm un mic blur global în timpul animației
        totalP.classList.add('rolling-fx');

        // Curățăm containerul și pregătim elementele pentru slot
        const chars = finalValue.split('');
        totalP.innerHTML = '';

        chars.forEach((char, index) => {
          if (/\d/.test(char)) {
            // Este o cifră, facem animația de slot
            const digitContainer = document.createElement('div');
            digitContainer.className = 'slot-digit';

            const inner = document.createElement('div');
            inner.className = 'slot-inner';

            const targetDigit = char;
            // Generăm mai multe cifre pentru o rotație mai lungă și mai rapidă
            const intermediateDigits = [];
            for (let i = 0; i < 8; i++) {
              intermediateDigits.push(Math.floor(Math.random() * 10));
            }
            const startDigit = Math.floor(Math.random() * 10);

            // Structura: [Target, ...Intermediare, Start]
            // Offset-ul va fi (intermediateDigits.length + 1) * 1.1em
            [targetDigit, ...intermediateDigits, startDigit].forEach(d => {
              const s = document.createElement('span');
              s.textContent = d;
              inner.appendChild(s);
            });

            // Setăm poziția inițială (ultima cifră din listă)
            inner.style.transform = `translateY(-${(intermediateDigits.length + 1) * 1.1}em)`;

            digitContainer.appendChild(inner);
            totalP.appendChild(digitContainer);

            // Declanșăm animația cu un delay per digit (staggered)
            setTimeout(() => {
              inner.style.transform = 'translateY(0)';
            }, index * 80 + 50);
          } else {
            // Nu e cifră (+, -, .), o punem ca text simplu
            const span = document.createElement('span');
            span.textContent = char;
            totalP.appendChild(span);
          }
        });

        // După ce s-a terminat animația (aprox 2.5s total pentru a cuprinde toate cifrele), revenim la text pur
        setTimeout(() => {
          totalP.textContent = finalValue;
          totalP.classList.remove('rolling-fx');
          _isRolling = false;

          if (isPos && window.Android && typeof Android.vibrate === 'function') {
            Android.vibrate(50);
          }
        }, 2500);
      }

      /* --- ANIMATE COUNTER (NUMERIC PROGRESSION v22.2) --- */
      let _isWRRolling = false;
      function animateCounter(el, target, suffix = '') {
        if (!el || _isWRRolling) return;
        _isWRRolling = true;

        const duration = 2000; // Crescut la 2 secunde pentru o animație mai lentă
        const startTime = performance.now();

        function update(currentTime) {
          const elapsed = currentTime - startTime;
          const progress = Math.min(elapsed / duration, 1);

          // easeOutQuart pentru o încetinire mult mai pronunțată la final
          const easedProgress = 1 - Math.pow(1 - progress, 4);
          const current = Math.floor(easedProgress * target);

          el.textContent = current + suffix;

          if (progress < 1) {
            requestAnimationFrame(update);
          } else {
            el.textContent = target + suffix;
            _isWRRolling = false; // Deblocăm după finalizare
          }
        }
        requestAnimationFrame(update);
      }

      function animateWinRateRoll() {
        const totalWr = document.getElementById('total-wr');
        if (!totalWr) return;
        const val = parseInt(totalWr.textContent) || 0;
        animateCounter(totalWr, val, '%');
      }

      function renderStatsOnly() {
        let p = portfolios.find(x => x.id === currentPortfolioId);
        let initialBudget = p ? p.budget : 1000;
        let profitOffset = p ? p.offset : 0;

        document.getElementById('budget-input').value = initialBudget;
        let netProfit = 0;
        let wins = 0;
        let settledCount = 0;
        let totalStaked = 0;

        const currentBets = getCurrentBets();

        currentBets.forEach(b => {
          if(b.status === 'win') {
            netProfit += (b.stake * b.odds) - b.stake;
            wins++;
            settledCount++;
            totalStaked += b.stake;
          } else if(b.status === 'loss') {
            netProfit -= b.stake;
            settledCount++;
            totalStaked += b.stake;
          } else if(b.status === 'cashout') {
            netProfit += (b.cashoutAmount - b.stake);
            wins++;
            settledCount++;
            totalStaked += b.stake;
          }
        });
        let displayedProfit = netProfit - profitOffset;

        const totalP = document.getElementById('total-p');
        if(totalP) {
          const val = (displayedProfit >= 0 ? '+' : '') + displayedProfit.toFixed(2);
          totalP.textContent = val;
          totalP.className = 'stat-val ' + (displayedProfit >= 0 ? 'pos' : 'neg');

          // Scalare dinamică font ultra-adaptivă (v9.1)
          let fs = 26; // Redus de la 28 pentru siguranță
          const len = val.length;
          if (len > 6)  fs = 22;
          if (len > 8)  fs = 19;
          if (len > 10) fs = 16;
          if (len > 12) fs = 13;
          if (len > 14) fs = 11;
          if (len > 16) fs = 9;
          totalP.style.fontSize = fs + 'px';
          totalP.style.whiteSpace = 'nowrap';
          totalP.style.overflow = 'hidden';
          totalP.style.textOverflow = 'clip';
        }

        const totalWr = document.getElementById('total-wr');
        if(totalWr) {
          const wrValue = settledCount > 0 ? Math.round((wins / settledCount) * 100) : 0;
          totalWr.textContent = wrValue + '%';
          animateCounter(totalWr, wrValue, '%');
        }

        const totalTicketsEl = document.getElementById('total-tickets');
        if (totalTicketsEl) {
          totalTicketsEl.textContent = currentBets.length;
        }

        renderBankrollHealth(initialBudget, displayedProfit, totalStaked);
        updateTargetMeter();

        // Sincronizare cu pagina de Profil (v16.88)
        if (typeof updateProfileStatsUI === 'function') {
            updateProfileStatsUI();
        }
      }

      /* ═══════════════════════════════════════════════
         BANKROLL HEALTH & ANALYTICS
         ROI       = Profit Net / Balanță Start  × 100  (randament fata de capitalul investit)
         Yield     = Profit Net / Total Mizat    × 100  (randament fata de cat s-a pariat efectiv)
         Max Drawdown = cea mai mare scadere (in RON) fata de un varf anterior
                        al soldului, parcurs cronologic prin biletele decise.
      ═══════════════════════════════════════════════ */
      function renderBankrollHealth(initialBudget, netProfit, totalStaked) {
        const roiEl      = document.getElementById('bh-roi');
        const yieldEl     = document.getElementById('bh-yield');
        const stakedEl    = document.getElementById('bh-staked');
        const drawdownEl  = document.getElementById('bh-drawdown');
        if (!roiEl || !yieldEl || !stakedEl || !drawdownEl) return;

        // ROI — fata de capitalul initial
        const roi = initialBudget > 0 ? (netProfit / initialBudget) * 100 : 0;
        roiEl.textContent = (roi >= 0 ? '' : '') + roi.toFixed(1) + '%';
        roiEl.className = 'bh-cell-val ' + (roi >= 0 ? 'pos' : 'neg');

        // Yield — fata de totalul mizat efectiv
        const yieldPct = totalStaked > 0 ? (netProfit / totalStaked) * 100 : 0;
        yieldEl.textContent = yieldPct.toFixed(1) + '%';
        yieldEl.className = 'bh-cell-val ' + (yieldPct >= 0 ? 'pos' : 'neg');

        // Total Mizat
        stakedEl.innerHTML = Math.round(totalStaked) + ' <span class="bh-unit">RON</span>';

        // Max Drawdown — calculat din seria cronologica de solduri
        const settledBets = getCurrentBets()
          .filter(b => b.status === 'win' || b.status === 'loss' || b.status === 'cashout')
          .sort((a, b) => a.id - b.id);

        let balance = initialBudget;
        let peak = initialBudget;
        let maxDrawdown = 0;
        settledBets.forEach(b => {
          if (b.status === 'win') balance += (b.stake * b.odds) - b.stake;
          else if (b.status === 'loss') balance -= b.stake;
          else if (b.status === 'cashout') balance += (b.cashoutAmount - b.stake);

          if (balance > peak) peak = balance;
          const drawdown = balance - peak; // valoare negativa sau 0
          if (drawdown < maxDrawdown) maxDrawdown = drawdown;
        });

        drawdownEl.innerHTML = Math.round(maxDrawdown) + ' <span class="bh-unit">RON</span>';
        drawdownEl.className = 'bh-cell-val ' + (maxDrawdown < 0 ? 'neg' : 'pos');
      }

      /* ── APEX TACTICAL HOLOGRAPHIC CHART (v7.0 PRO) ── */
      function initChartParticles() {
        const container = document.getElementById('chartParticles');
        if (!container) return;
        container.innerHTML = '';
        for (let i = 0; i < 25; i++) {
          const bit = document.createElement('div');
          bit.className = 'data-bit';
          bit.style.left = Math.random() * 100 + '%';
          bit.style.top = Math.random() * 100 + '%';
          bit.style.animationDelay = Math.random() * 5 + 's';
          bit.textContent = Math.random() > 0.5 ? '0' : '1';
          container.appendChild(bit);
        }
      }

      function updateChart() {
        const canvas = document.getElementById('mainChart');
        if (!canvas || typeof Chart === 'undefined') return;

        const ctx = canvas.getContext('2d');
        let p = portfolios.find(x => x.id === currentPortfolioId) || { budget: 1000 };
        let settledBets = getCurrentBets().filter(b => b.status === 'win' || b.status === 'loss' || b.status === 'cashout');
        settledBets.sort((a, b) => a.id - b.id);

        let balanceData = [p.budget];
        let pnlData = [0];
        let labels = ['START'];
        let currentBalance = p.budget;

        settledBets.forEach((b, idx) => {
          let pnl = b.status === 'win' ? (b.stake * b.odds) - b.stake : (b.status === 'loss' ? -b.stake : (b.cashoutAmount - b.stake));
          currentBalance += pnl;
          balanceData.push(currentBalance);
          pnlData.push(pnl);
          labels.push(b.date ? new Date(b.date).toLocaleDateString('ro-RO', {day:'2-digit', month:'short'}) : `#${idx+1}`);
        });

        if (chartInstance) chartInstance.destroy();

        const lineColor = currentWallpaperMode === 'gold' ? '#ffcc00' : '#00FAFF';
        const rgb = lineColor === '#ffcc00' ? '255, 204, 0' : '0, 250, 255';

        // HUD & Tactical Plugin
        const tacticalHudPlugin = {
          id: 'tacticalHud',
          beforeDraw: (chart) => {
            const {ctx, chartArea: {top, bottom, left, right, width, height}} = chart;
            ctx.save();
            // 1. Cyber Dot Grid
            ctx.fillStyle = `rgba(${rgb}, 0.04)`;
            for(let x=left; x<=right; x+=20) for(let y=top; y<=bottom; y+=20) { ctx.beginPath(); ctx.arc(x, y, 1, 0, Math.PI*2); ctx.fill(); }
            ctx.restore();
          },
          afterDraw: (chart) => {
            if (chart.tooltip?._active?.length) {
              const {ctx, chartArea: {top, bottom, left, right}} = chart;
              const activePoint = chart.tooltip._active[0];
              const {x, y} = activePoint.element;
              ctx.save();
              ctx.setLineDash([5, 5]); ctx.strokeStyle = `rgba(${rgb}, 0.4)`; ctx.lineWidth = 1;
              // Crosshair vertical
              ctx.beginPath(); ctx.moveTo(x, top); ctx.lineTo(x, bottom); ctx.stroke();
              // Crosshair horizontal
              ctx.beginPath(); ctx.moveTo(left, y); ctx.lineTo(right, y); ctx.stroke();
              ctx.restore();
            }
          }
        };

        const gradient = ctx.createLinearGradient(0, 0, 0, 220);
        gradient.addColorStop(0, `rgba(${rgb}, 0.4)`);
        gradient.addColorStop(0.6, `rgba(${rgb}, 0.05)`);
        gradient.addColorStop(1, 'transparent');

        chartInstance = new Chart(canvas, {
          type: 'line',
          plugins: [tacticalHudPlugin],
          data: {
            labels: labels,
            datasets: [
              {
                label: 'NET ASSETS',
                data: balanceData,
                borderColor: lineColor,
                borderWidth: 4,
                pointRadius: 4,
                pointBackgroundColor: '#fff',
                pointBorderColor: lineColor,
                pointBorderWidth: 2,
                pointHoverRadius: 8,
                pointHoverBackgroundColor: lineColor,
                pointHoverBorderColor: '#fff',
                tension: 0.4,
                fill: true,
                backgroundColor: gradient,
                yAxisID: 'y'
              },
              {
                label: 'PnL DELTA',
                data: pnlData,
                type: 'bar',
                backgroundColor: pnlData.map(v => v >= 0 ? 'rgba(0, 255, 136, 0.15)' : 'rgba(255, 51, 102, 0.15)'),
                borderColor: pnlData.map(v => v >= 0 ? 'rgba(0, 255, 136, 0.3)' : 'rgba(255, 51, 102, 0.3)'),
                borderWidth: 1,
                borderRadius: 4,
                barThickness: (ctx) => Math.max(2, 120 / labels.length),
                yAxisID: 'y1'
              }
            ]
          },
          options: {
            responsive: true, maintainAspectRatio: false,
            animation: { duration: 1800, easing: 'easeOutQuart' },
            plugins: {
              legend: { display: false },
              tooltip: {
                backgroundColor: 'rgba(0, 5, 15, 0.96)',
                borderColor: lineColor,
                borderWidth: 1,
                titleFont: { family: 'Syncopate', size: 11, weight: '700' },
                bodyFont: { family: 'Rajdhani', size: 16, weight: '700' },
                padding: 15,
                displayColors: false,
                callbacks: {
                  label: (ctx) => `VAL: ${ctx.parsed.y.toFixed(2)} ${getCurrency()}`
                }
              }
            },
            scales: {
              x: {
                grid: { display: false },
                ticks: { color: `rgba(${rgb}, 0.6)`, font: { family: 'Syncopate', size: 9 }, autoSkip: true, maxTicksLimit: 4, maxRotation: 0 }
              },
              y: {
                position: 'left',
                grid: { color: `rgba(${rgb}, 0.05)`, drawBorder: false },
                ticks: { color: `rgba(${rgb}, 0.8)`, font: { family: 'Rajdhani', size: 12, weight: '700' }, callback: (v) => v.toLocaleString() }
              },
              y1: { display: false, position: 'right', min: Math.min(...pnlData) * 4, max: Math.max(...pnlData) * 4 }
            },
            interaction: { intersect: false, mode: 'index' }
          }
        });

        initChartParticles();
      }

      function updateSportChart() {
        const ctx = document.getElementById('sportChart');
        if (!ctx) return;
        if (typeof Chart === 'undefined') {
          console.warn('Chart.js nu este încărcat — graficul pe sporturi este omis temporar.');
          return;
        }

        const currentBets = getCurrentBets();
        let sportStats = {
          football: 0,
          tennis: 0,
          basketball: 0,
          other: 0
        };

        currentBets.forEach(b => {
          let pnl = 0;
          if (b.status === 'win') pnl = (b.stake * b.odds) - b.stake;
          else if (b.status === 'loss') pnl = -b.stake;
          else if (b.status === 'cashout') pnl = b.cashoutAmount - b.stake;

          if (sportStats.hasOwnProperty(b.sport)) sportStats[b.sport] += pnl;
          else sportStats.other += pnl;
        });

        const data = {
          labels: [t('sport_foot_short'), t('sport_ten_short'), t('sport_basket_short'), t('sport_other_short')],
          datasets: [{
            data: [sportStats.football, sportStats.tennis, sportStats.basketball, sportStats.other],
            backgroundColor: [
              'rgba(0, 255, 136, 0.6)', // Green
              'rgba(0, 200, 255, 0.6)', // Blue
              'rgba(255, 204, 0, 0.6)',  // Gold
              'rgba(191, 95, 255, 0.6)'  // Purple
            ],
            borderColor: ['#00ff88', '#00c8ff', '#ffcc00', '#bf5fff'],
            borderWidth: 1,
            hoverOffset: 10
          }]
        };

        if (sportChartInstance) sportChartInstance.destroy();

        sportChartInstance = new Chart(ctx, {
          type: 'doughnut',
          data: data,
          options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
              legend: {
                position: 'bottom',
                labels: { color: '#fff', font: { family: 'Rajdhani', size: 11 } }
              },
              tooltip: {
                callbacks: {
                  label: (item) => `${item.label}: ${(item.raw || 0).toFixed(2)} ${getCurrency()}`
                }
              }
            },
            cutout: '70%'
          }
        });
      }

      /* --- FUNCȚIA DE OBSERVARE PENTRU ANIMAȚIE SCROLL --- */
      function setupScrollAnimations() {
        const observer = new IntersectionObserver((entries, observerInstance) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('active');
              observerInstance.unobserve(entry.target);
            }
          });
        }, { threshold: 0.05 });

        document.querySelectorAll('.bet-item.scroll-animate').forEach(el => {
          observer.observe(el);
          // Detectare imediată dacă deja e în vizor
          const rect = el.getBoundingClientRect();
          if (rect.top < window.innerHeight && rect.bottom > 0) {
            el.classList.add('active');
          }
        });
      }

      function render() {
        try {
          // Sincronizare cu datele din LocalStorage (Cloud/Native Sync)
          const storedBets = localStorage.getItem('rgb_bets');
          bets = storedBets ? JSON.parse(storedBets) : [];

          const storedPortfolios = localStorage.getItem('rgb_portfolios');
          portfolios = storedPortfolios ? JSON.parse(storedPortfolios) : [];

          currentPortfolioId = localStorage.getItem('rgb_current_portfolio') || 'default';

          try { renderStatsOnly(); } catch(e) { console.error("Stats error", e); }
          try { renderStreakFlames(); } catch(e) { console.error("Streak error", e); }
          try { renderProfitCalendar(); } catch(e) { console.error("Calendar error", e); }
          try { updateSportChart(); } catch(e) { console.error("SportChart error", e); }

          const formCircles = document.getElementById('form-circles');
        if(formCircles) {
          formCircles.innerHTML = '';
          let allSettled = getCurrentBets().filter(b => b.status === 'win' || b.status === 'loss' || b.status === 'cashout');

          // Streak Calculation
          let winStreak = 0;
          let loseStreak = 0;

          if (allSettled.length > 0) {
            let i = allSettled.length - 1;
            const lastStatus = allSettled[i].status;

            if (lastStatus === 'win' || lastStatus === 'cashout') {
              while (i >= 0 && (allSettled[i].status === 'win' || allSettled[i].status === 'cashout')) {
                winStreak++;
                i--;
              }
            } else if (lastStatus === 'loss') {
              while (i >= 0 && allSettled[i].status === 'loss') {
                loseStreak++;
                i--;
              }
            }
          }

          let recentForm = allSettled.slice(-3);
          recentForm.forEach(b => {
            if(b.status === 'win' || b.status === 'cashout') {
              formCircles.innerHTML += `<span class="fs-badge fs-w">W</span>`;
            } else if(b.status === 'loss') {
              formCircles.innerHTML += `<span class="fs-badge fs-l">L</span>`;
            }
          });

          if(allSettled.length === 0) {
            formCircles.innerHTML = '<span style="color:var(--text3); font-size:11px;">-</span>';
          } else if (winStreak > 0) {
             formCircles.innerHTML += `<div class="win-streak-badge"><i class="fa-solid fa-fire"></i> ${winStreak}</div>`;
          } else if (loseStreak > 0) {
             formCircles.innerHTML += `<div class="lose-streak-badge"><i class="fa-solid fa-snowflake"></i> ${loseStreak}</div>`;
          }
        }

        const container = document.getElementById('betsContainer');
        if(!container) return;
        container.innerHTML = '';

        // Utilizăm toate biletele pentru listă (conform cerinței de a vedea și cele anterioare)
        let filteredBets = bets.filter(b => {
          const matchesFilter = (currentFilter === 'all') || (b.status === currentFilter);
          const matchesSport = (currentSportFilter === 'all') || (b.sport === currentSportFilter);
          return matchesFilter && matchesSport;
        });
        let displayBets = [...filteredBets].reverse();

        if(displayBets.length === 0) {
          container.innerHTML = `<div class="no-bets">${t('no_tickets')}</div>`;
          document.getElementById('paginationContainer').innerHTML = ''; // Ascunde paginația
          updateChart();
          return;
        }

        // Paginație Logic
        const totalPages = Math.ceil(displayBets.length / itemsPerPage);
        if (currentPagPage > totalPages) currentPagPage = totalPages || 1;

        const startIndex = (currentPagPage - 1) * itemsPerPage;
        const paginatedBets = displayBets.slice(startIndex, startIndex + itemsPerPage);

        paginatedBets.forEach(b => {
          let actionDockHtml = '';
          let statusClass = '';

          if(b.status === 'pending') {
            actionDockHtml = `
              <div class="apex-action-dock">
                <button class="dock-btn dock-btn-win" onclick="changeStatus(${b.id}, 'win')">
                  <i class="fa-solid fa-check-double"></i>
                  <span>WIN</span>
                </button>
                <button class="dock-btn dock-btn-cash" onclick="cashoutBet(${b.id})">
                  <i class="fa-solid fa-hand-holding-dollar"></i>
                  <span>CASH</span>
                </button>
                <button class="dock-btn dock-btn-loss" onclick="changeStatus(${b.id}, 'loss')">
                  <i class="fa-solid fa-xmark"></i>
                  <span>LOSS</span>
                </button>
              </div>
            `;
          } else {
            const bandClass = b.status === 'win' ? 'band-win' : (b.status === 'loss' ? 'band-loss' : 'band-cash');
            const bandIcon = b.status === 'win' ? 'fa-circle-check' : (b.status === 'loss' ? 'fa-circle-xmark' : 'fa-hand-holding-dollar');
            const bandText = b.status === 'win' ? (currentLang === 'ro' ? 'BILET CÂȘTIGĂTOR' : 'WINNING TICKET') :
                             (b.status === 'loss' ? (currentLang === 'ro' ? 'BILET PIERDUT' : 'LOST TICKET') : 'CASHOUT CONFIRMED');

            actionDockHtml = `
              <div class="decided-status-band ${bandClass}">
                <i class="fa-solid ${bandIcon}"></i>
                <span>${bandText}</span>
                <button class="undo-btn" onclick="makeStatusPending(${b.id})" title="Undo">
                  <i class="fa-solid fa-rotate-left"></i>
                </button>
              </div>
            `;

            if(b.status === 'win' || b.status === 'cashout') statusClass = 'win-anim';
          }

          let dateStr = '-';
          if (b.date) {
            const d = new Date(b.date);
            dateStr = d.toLocaleDateString(currentLang === 'ro' ? 'ro-RO' : 'en-GB', { day: '2-digit', month: 'short' });
          }

          const statusLabel = b.status === 'win' ? (currentLang === 'ro' ? 'CÂȘTIGĂTOR' : 'WINNER') :
                             b.status === 'loss' ? (currentLang === 'ro' ? 'PIERDUT' : 'LOST') :
                             b.status === 'cashout' ? (currentLang === 'ro' ? 'CASHOUT' : 'CASHOUT') :
                             (currentLang === 'ro' ? 'ÎN AȘTEPTARE' : 'PENDING');

          const stampClass = b.status === 'win' ? 'stamp-win' :
                            b.status === 'loss' ? 'stamp-loss' :
                            b.status === 'cashout' ? 'stamp-win' : 'stamp-pending';

          // Realistic Events Layout
          let eventsRealisticHtml = '';
          if (b.events && b.events.length > 0) {
            const grouped = {};
            b.events.forEach(ev => {
              if (!grouped[ev.name]) grouped[ev.name] = [];
              grouped[ev.name].push(ev);
            });

            Object.keys(grouped).forEach(matchName => {
              const matchEvents = grouped[matchName];
              const leagueName = matchEvents[0].league || "";
              const eventTime = matchEvents[0].time || "";
              eventsRealisticHtml += `
                <div class="soc-event-row">
                  ${leagueName ? `<div style="font-size:9px; color:#888; text-transform:uppercase; font-weight:700; margin-bottom:2px; letter-spacing:0.5px;">${leagueName}</div>` : ''}
                  <span class="soc-ev-match" style="color:#000;">${eventTime ? `<span style="color:#333; margin-right:6px; font-weight:800;">${eventTime}</span>` : ''}⚽ ${matchName}</span>
                  ${matchEvents.map(ev => `
                    <div class="soc-ev-market-line">
                      <span class="soc-ev-market" style="color:#555;">${ev.market || (currentLang === 'ro' ? 'Pronostic' : 'Market')}</span>
                      <span class="soc-ev-odds" style="color:#000;">@${(ev.odds || 0).toFixed(2)}</span>
                    </div>
                  `).join('')}
                </div>`;
            });
          }

          container.innerHTML += `
            <div class="bet-item scroll-animate ${statusClass} bet-status-${b.status}">
              <div class="soc-ticket-realistic" style="margin:0;">
                <div class="soc-ticket-header">
                  <span class="soc-ticket-brand" style="font-family:'Rajdhani', sans-serif; font-weight:900; font-size:22px; letter-spacing:2px;">rGdbet</span>
                  <div style="display:flex; align-items:center; gap:10px;">
                    <button class="ticket-share-btn" onclick="if(window.shareTicket) shareTicket('${b.id}'); else alert('Loading...');">
                      <i class="fa-solid fa-share-nodes"></i>
                    </button>
                    <span class="soc-ticket-stamp ${stampClass}">${statusLabel}</span>
                  </div>
                </div>

                <div class="soc-ticket-body">
                  ${eventsRealisticHtml || `
                    <div class="soc-event-row">
                      <span class="soc-ev-match" style="color:#000;">⚽ ${b.name || t('ticket')}</span>
                      <div class="soc-ev-market-line">
                        <span class="soc-ev-market" style="color:#555;">${currentLang === 'ro' ? 'Pronostic' : 'Prediction'}</span>
                        <span class="soc-ev-odds" style="color:#000;">@${(b.odds || 0).toFixed(2)}</span>
                      </div>
                    </div>`}
                </div>

                <div class="soc-ticket-footer" style="border-top: 1px dashed #000; padding-top:15px; margin-top:15px;">
                  <div class="soc-total-odds-box">
                    <div class="soc-total-lbl">COTĂ TOTALĂ</div>
                    <div class="soc-total-val" style="color:#000;">@${(b.odds || 0).toFixed(2)}</div>
                  </div>
                  <div style="text-align:right;">
                    <div class="soc-total-lbl">${t('miza').toUpperCase()}</div>
                    <div style="font-weight:900; font-size:18px; color:#000;">${b.stake} ${getCurrency()}</div>
                  </div>
                </div>

                <div class="soc-ticket-footer" style="border:none; margin-top:5px; padding-top:0;">
                   <div class="soc-total-odds-box">
                      <div class="soc-total-lbl">${currentLang === 'ro' ? 'CÂȘTIG POSIBIL' : 'POTENTIAL PAYOUT'}</div>
                      <div style="font-weight:900; font-size:20px; color:${b.status === 'win' || b.status === 'pending' ? '#2e7d32' : b.status === 'loss' ? '#d32f2f' : '#b8860b'};">
                        ${b.status === 'cashout' ? (b.cashoutAmount || 0).toFixed(2) : (b.status === 'loss' ? '0.00' : (b.stake * b.odds).toFixed(2))} ${getCurrency()}
                      </div>
                   </div>
                   <div style="text-align:right;">
                      <div class="soc-total-lbl">DATA</div>
                      <div style="font-weight:700; font-size:12px; color:#666;">${dateStr}</div>
                   </div>
                </div>

                <div class="soc-barcode-area">
                  <div class="soc-barcode"></div>
                  <div class="soc-ticket-id">REF-${b.id}</div>
                </div>
              </div>

              ${b.photo ? `<div class="bet-photo-container" style="margin-top:10px; padding:0 10px 10px;"><img src="${b.photo}" class="bet-thumb" onclick="viewFullImage('${b.photo}')" style="width:100%; border-radius:15px; border:2px solid rgba(255,255,255,0.1);"></div>` : ''}

              ${actionDockHtml}
            </div>
          `;
        });

        updateChart();
        setupScrollAnimations();
        renderPagination(totalPages);
        } catch (err) {
          console.error('Eroare în render():', err);
        }
      }

      function renderPagination(totalPages) {
        const container = document.getElementById('paginationContainer');
        if (!container) return;

        if (totalPages <= 1) {
          container.innerHTML = '';
          return;
        }

        let html = `
          <button class="pag-btn ${currentPagPage === 1 ? 'disabled' : ''}" onclick="changePage(${currentPagPage - 1})">
            <i class="fa-solid fa-chevron-left"></i>
          </button>
          <div class="pag-info">${currentPagPage} / ${totalPages}</div>
          <button class="pag-btn ${currentPagPage === totalPages ? 'disabled' : ''}" onclick="changePage(${currentPagPage + 1})">
            <i class="fa-solid fa-chevron-right"></i>
          </button>
        `;
        container.innerHTML = html;
      }

      function changePage(page) {
        currentPagPage = page;
        render();
        // Scroll lin înapoi la începutul listei de bilete
        document.querySelector('[data-i18n="active_hist"]').scrollIntoView({ behavior: 'smooth' });
      }

      window.openUpgradeAction = function() {
        if (window.Android && typeof window.Android.purchasePremium === 'function') {
          window.Android.purchasePremium();
        } else {
          // Fallback for web/testing
          if (typeof navigateTo === 'function') {
            navigateTo('profile', null);
            setTimeout(() => {
              const el = document.getElementById('premium-plans-section');
              if (el) el.scrollIntoView({ behavior: 'smooth' });
            }, 500);
          }
        }
      };